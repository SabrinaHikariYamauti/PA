import * as React from 'react';
import { TextInput, View } from 'react-native';

import Previsao from '../Components/Previsao';

const TelaPrevisao = ({ navigation }) => {
    return (
        <View>
            <Previsao />
        </View >
    );
}

export default TelaPrevisao;