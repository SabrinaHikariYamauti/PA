import { StyleSheet } from 'react-native';

const ControlesStyles = StyleSheet.create({
    Controle: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center'
    },
    ControleAltura: {
        alignItems: 'center',
        justifyContent: 'center'

    },
    TextButton: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#A1A5AB'
    },
    ControleCooler: {
        alignItems: 'center',
        justifyContent: 'center'
    },

});

export default ControlesStyles;